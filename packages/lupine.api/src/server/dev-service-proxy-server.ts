import { IDevServiceProxy } from '../models';
// The handlers should be copied from ApiModule (from api to server), so server can call those services in api
export const DevServiceProxyServer: IDevServiceProxy = {};
