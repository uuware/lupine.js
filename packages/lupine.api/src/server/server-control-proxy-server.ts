import { IServerControlProxy } from '../models';
// The handlers should be copied to ApiModule (from server to api), so api can call those services in server
export const ServerControlProxyServer: IServerControlProxy = {};
