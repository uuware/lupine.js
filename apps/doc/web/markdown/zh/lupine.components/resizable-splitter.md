---
title: ResizableSplitter (可调分割器)
---

# ResizableSplitter (可调分割器)

可拖拉调整两个组件大小的分割器。

## 实装例子

```typescript
import { ResizableSplitter } from 'lupine.components';

<ResizableSplitter left={<div>左侧内容</div>} right={<div>右侧内容</div>} minWidth='200px' />;
```
