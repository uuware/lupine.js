---
title: SvgIcon (SVG 图标)
---

# SvgIcon (SVG 图标)

高性能原生 SVG 图标组件，支持通过路径或名称显示图标。

## 实装例子

```typescript
import { SvgIcon } from 'lupine.components';

<SvgIcon name='home' size={24} />
<SvgIcon name='settings' color='red' />
```
