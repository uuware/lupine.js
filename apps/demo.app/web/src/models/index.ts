export enum UserType {
  Student = 'student',
  Teacher = 'teacher',
  Admin = 'admin',
}

export enum QuestionState {
  Question = '0',
  Answer = '1',
  Feedback = '2',
  Accept = '3',
}

export const tokenCookieName = '_token';
export const userCookieName = '_name';
